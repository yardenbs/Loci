package com.mta.loci;

class Post {
    //members: UserId, Location, Media (Interface)
    String UserId;

    Post(String UserId) {
        //add inputs for c'tor
        this.UserId = UserId;
    }
}
